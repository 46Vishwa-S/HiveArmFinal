name = "hivearmcontrol"
compatibility_date = "2026-06-05"
compatibility_flags = ["nodejs_compat"]

# Tell wrangler this is a static assets deployment (Cloudflare Pages)
# This prevents wrangler deploy from requiring a Worker entry-point
[assets]
directory = "./dist/client"
